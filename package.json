{
  "name": "visualink",
  "version": "0.0.1",
  "description": "Visualize and interact with Linked Lists",
  "main": "./public/index.js",
  "scripts": {
    "start": "npm run tsc && npm run css && npm run live",
    "dev": "npm run tsc && concurrently \"npm run css-watch\" \"npm run live\"",
    "css": "sass ./src/sass/main.scss ./public/index.css",
    "css-watch": "sass --watch ./src/sass/main.scss ./public/index.css",
    "live": "live-server --no-css-inject ./public/",
    "tsc": "tsc --project tsconfig.json"
  },
  "keywords": [],
  "author": "Patrick (rodpadev) Rodrigues ",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^5.3.0",
    "live-server": "^1.2.1",
    "sass": "^1.27.1",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "konva": "^7.1.4"
  }
}
